{{#with data.courseDetails}}
<div class="container">
    <h1 class="my-4">Course title : {{title}}
    </h1>

    <div class="row">

        <div class="col-md-8">
            <img class="img-fluid"
                src="{{imageUrl}}" alt="JavaScript" />
        </div>

        <div class="col-md-4 text-center">
            <h3 class="my-3">Course Description</h3>
            <p>{{description}}</p>
{{!-- add hbs db id --}}
            {{!-- <a class="btn btn-danger" href="/course/delete">Delete</a> 
            <a class="btn btn-warning" href="/course/edit">Edit</a> if user created course --}}
            {{!-- <span class="enrolled-span">You are already enrolled</span> if user enrolled --}}
            <a class="btn btn-primary" href="/course/enroll/{{_id}}">Enroll</a> 

        </div>
    </div>
</div>
{{/with}}